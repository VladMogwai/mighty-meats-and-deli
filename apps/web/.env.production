# Public production URLs, read by `next build` (not secrets — they appear in the page HTML).
# Committed so the Cloudflare Pages build does not depend on dashboard variables;
# a variable set in the build environment still takes precedence.
CMS_URL=https://mighty-meats-cms.onrender.com
NEXT_PUBLIC_CMS_URL=https://mighty-meats-cms.onrender.com
NEXT_PUBLIC_SITE_URL=https://mighty-meats-and-deli.pages.dev
